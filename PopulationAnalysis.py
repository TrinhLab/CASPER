"""Classes for generating and analyzing a population."""

class PopulationGenerator:

    def __init__(self):
        # Data storage containers
        self.pop_directory = str()
        self.csprfiles = list()
        self.


    def generate(self,processes):
        for item in processes:
            if item == "Count":
                poo = 1
            elif item == ""

    def VennDiagram(self):




P = PopulationGenerator()